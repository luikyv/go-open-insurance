overlay: 1.0.0
info:
  title: Overlay specification for Open Insurance APIs.
  version: 1.0.0

actions:
- target: "$.components.schemas..*[?(@.format == 'date-time')]"
  update:
    x-go-type: DateTime